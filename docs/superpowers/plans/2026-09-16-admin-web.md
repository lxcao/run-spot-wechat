# 跑团网页后台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在仓库根目录新增与 `h5/`、`miniprogram/` 同级的 `admin/` 网站，让管理员用用户名密码登录后，对 CloudBase 里的活动做增删改查并管理照片。

**Architecture:** 浏览器经 `@cloudbase/js-sdk` 用户名密码登录；读走现有 `getEvents`；写走云函数。云函数用共享 `assertAdmin`：小程序 openid ∈ `team.admins` 或网站 uid ∈ `team.webAdmins`。网站部署到独立应用域名，不覆盖 H5。

**Tech Stack:** Vite 5 + React 18 + TypeScript；`@cloudbase/js-sdk`；现有微信云函数（`wx-server-sdk` + `@cloudbase/node-sdk` 读 uid）；Vitest + `node:test`。

**Spec:** `docs/superpowers/specs/2026-09-16-admin-web-design.md`

---

## File map

**云函数（`miniprogram/cloudfunctions/`）**

| 路径 | 职责 |
|---|---|
| `_shared/assertAdmin.js` | 权限判定纯逻辑 + 云端装配（canonical） |
| `_shared/assertAdmin.test.js` | `node:test` 单测 |
| `_shared/copy-assert-admin.sh` | 把 canonical 文件复制进各函数目录 |
| `createEvent/assertAdmin.js` | 部署副本（由脚本生成，勿手改） |
| `deleteEvent/assertAdmin.js` | 同上 |
| `updateEventPhotos/assertAdmin.js` | 同上 |
| `updateEvent/` | 新函数：改活动，不可改 id/date |
| `deletePhoto/` | 新函数：删单张照片 |

**网站（`admin/`，与 h5、miniprogram 同级）**

| 路径 | 职责 |
|---|---|
| `package.json` `vite.config.ts` `tsconfig.json` `index.html` | 脚手架 |
| `src/lib/cloudbase.ts` | SDK init（env + region + accessKey） |
| `src/lib/auth.ts` | `getSession` 守卫，禁止匿名 / `getLoginState` |
| `src/lib/api.ts` | `callFunction` 封装，返回 `{ code, msg, data }` |
| `src/lib/events.ts` | 状态拆分、自动 status、禁止改日期 |
| `src/types.ts` | Event / Photo / Team |
| `src/pages/LoginPage.tsx` | 登录，无注册 |
| `src/pages/UnauthorizedPage.tsx` | 展示并复制 uid |
| `src/pages/EventListPage.tsx` | 列表 + 筛选 |
| `src/pages/EventFormPage.tsx` | 新建/编辑 + 照片（编辑时） |
| `src/components/AddressSuggest.tsx` | 高德联想 |
| `src/components/PhotoGrid.tsx` | 缩略图、上传、删除 |
| `src/App.tsx` `src/main.tsx` `src/styles.css` | 路由与主题 |

不要改 `h5/`。不要改小程序页面（⚙️ 配置页保留）。

---

### Task 1: assertAdmin 纯逻辑 + 测试

**Files:**
- Create: `miniprogram/cloudfunctions/_shared/assertAdmin.js`
- Create: `miniprogram/cloudfunctions/_shared/assertAdmin.test.js`

- [ ] **Step 1: 写失败测试**

```js
// miniprogram/cloudfunctions/_shared/assertAdmin.test.js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { decideAdmin } = require('./assertAdmin');

const team = {
  admins: ['mp-openid-1'],
  webAdmins: ['web-uid-1'],
};

test('mini program openid in admins is mp admin', () => {
  const r = decideAdmin({ openid: 'mp-openid-1', uid: null, team });
  assert.equal(r.ok, true);
  assert.equal(r.kind, 'mp');
  assert.equal(r.id, 'mp-openid-1');
});

test('web uid in webAdmins is web admin', () => {
  const r = decideAdmin({ openid: null, uid: 'web-uid-1', team });
  assert.equal(r.ok, true);
  assert.equal(r.kind, 'web');
  assert.equal(r.id, 'web-uid-1');
});

test('openid wins when both present and openid is admin', () => {
  const r = decideAdmin({ openid: 'mp-openid-1', uid: 'web-uid-1', team });
  assert.equal(r.kind, 'mp');
});

test('unknown caller is denied', () => {
  const r = decideAdmin({ openid: 'x', uid: 'y', team });
  assert.equal(r.ok, false);
  assert.equal(r.code, -1);
  assert.match(r.msg, /未授权/);
});

test('missing team fields are treated as empty arrays', () => {
  const r = decideAdmin({ openid: 'mp-openid-1', uid: null, team: {} });
  assert.equal(r.ok, false);
});

test('permission check failure must deny, never skip', () => {
  const r = decideAdmin({ openid: null, uid: null, team: null });
  assert.equal(r.ok, false);
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `node --test miniprogram/cloudfunctions/_shared/assertAdmin.test.js`

Expected: `Cannot find module './assertAdmin'` 或 `decideAdmin is not a function`

- [ ] **Step 3: 实现**

```js
// miniprogram/cloudfunctions/_shared/assertAdmin.js
function decideAdmin({ openid, uid, team }) {
  if (!team || typeof team !== 'object') {
    return { ok: false, code: -1, msg: '未授权：请联系群主把你加入管理员列表' };
  }
  const admins = Array.isArray(team.admins) ? team.admins : [];
  const webAdmins = Array.isArray(team.webAdmins) ? team.webAdmins : [];
  if (openid && admins.includes(openid)) {
    return { ok: true, kind: 'mp', id: openid };
  }
  if (uid && webAdmins.includes(uid)) {
    return { ok: true, kind: 'web', id: uid };
  }
  return { ok: false, code: -1, msg: '未授权：请联系群主把你加入管理员列表' };
}

async function loadTeam(db) {
  const res = await db.collection('team').where({ id: 'meta' }).limit(1).get();
  return res.data[0] || {};
}

function readOpenId(cloud) {
  try {
    return cloud.getWXContext().OPENID || null;
  } catch (err) {
    return null;
  }
}

function readUid() {
  try {
    const tcb = require('@cloudbase/node-sdk');
    const app = tcb.init({ env: tcb.SYMBOL_CURRENT_ENV });
    const info = app.auth().getUserInfo() || {};
    return info.uid || null;
  } catch (err) {
    return null;
  }
}

async function assertAdmin(cloud, db) {
  let team;
  try {
    team = await loadTeam(db);
  } catch (err) {
    return { ok: false, code: -1, msg: '未授权：请联系群主把你加入管理员列表' };
  }
  return decideAdmin({
    openid: readOpenId(cloud),
    uid: readUid(),
    team,
  });
}

module.exports = { decideAdmin, assertAdmin };
```

- [ ] **Step 4: 再跑测试**

Run: `node --test miniprogram/cloudfunctions/_shared/assertAdmin.test.js`

Expected: 全部 PASS（6 tests）

- [ ] **Step 5: Commit**

```bash
git add miniprogram/cloudfunctions/_shared/assertAdmin.js miniprogram/cloudfunctions/_shared/assertAdmin.test.js
git commit -m "feat(admin): add shared assertAdmin for mp openid and web uid"
```

---

### Task 2: 复制脚本 + 接到 createEvent / deleteEvent

**Files:**
- Create: `miniprogram/cloudfunctions/_shared/copy-assert-admin.sh`
- Modify: `miniprogram/cloudfunctions/createEvent/index.js`（权限块换成 `assertAdmin`）
- Modify: `miniprogram/cloudfunctions/createEvent/package.json`（加 `@cloudbase/node-sdk`）
- Modify: `miniprogram/cloudfunctions/deleteEvent/index.js`（权限 catch 不再放行）
- Modify: `miniprogram/cloudfunctions/deleteEvent/package.json`

- [ ] **Step 1: 复制脚本**

```bash
#!/usr/bin/env bash
# miniprogram/cloudfunctions/_shared/copy-assert-admin.sh
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
SRC="$ROOT/_shared/assertAdmin.js"
for fn in createEvent deleteEvent updateEventPhotos updateEvent deletePhoto; do
  dest="$ROOT/$fn"
  mkdir -p "$dest"
  cp "$SRC" "$dest/assertAdmin.js"
done
echo "copied assertAdmin.js into function folders"
```

Run: `chmod +x miniprogram/cloudfunctions/_shared/copy-assert-admin.sh && bash miniprogram/cloudfunctions/_shared/copy-assert-admin.sh`

Expected: `copied assertAdmin.js into function folders`

- [ ] **Step 2: createEvent 换权限**

在 `createEvent/index.js` 顶部已有 `const db = cloud.database();` 之后加上：

```js
const { assertAdmin } = require('./assertAdmin');
```

删掉现有这段（约 65–72 行）：

```js
  const wxContext = cloud.getWXContext();
  const OPENID = wxContext.OPENID;
  const teamRes = await db.collection('team').where({ id: 'meta' }).limit(1).get();
  const admins = teamRes.data[0]?.admins || [];
  if (!Array.isArray(admins) || !admins.includes(OPENID)) {
    return { code: -1, msg: '未授权：请联系群主把你加入管理员列表' };
  }
```

换成：

```js
  const gate = await assertAdmin(cloud, db);
  if (!gate.ok) {
    return { code: gate.code, msg: gate.msg };
  }
```

`package.json` dependencies 增加：`"@cloudbase/node-sdk": "~3.10.0"`（与 `wx-server-sdk` 并列）。

- [ ] **Step 3: deleteEvent 修 catch 放行**

同样 `require('./assertAdmin')`。把 15–26 行整段（含 `try/catch` 里 `console.warn` 后继续删）换成：

```js
  const gate = await assertAdmin(cloud, db);
  if (!gate.ok) {
    return { code: gate.code, msg: gate.msg };
  }
```

权限失败必须 `return`，后面的删库代码不得执行。`package.json` 同样加 `@cloudbase/node-sdk`。

- [ ] **Step 4: 语法检查**

Run:

```bash
node --check miniprogram/cloudfunctions/createEvent/index.js
node --check miniprogram/cloudfunctions/deleteEvent/index.js
node --check miniprogram/cloudfunctions/createEvent/assertAdmin.js
```

Expected: 无输出，exit 0

- [ ] **Step 5: Commit**

```bash
git add miniprogram/cloudfunctions/_shared/copy-assert-admin.sh \
  miniprogram/cloudfunctions/createEvent miniprogram/cloudfunctions/deleteEvent \
  miniprogram/cloudfunctions/updateEvent miniprogram/cloudfunctions/deletePhoto \
  miniprogram/cloudfunctions/updateEventPhotos/assertAdmin.js
git commit -m "fix(cloud): assertAdmin on create/delete; never skip auth errors"
```

---

### Task 3: 云函数 `updateEvent`

**Files:**
- Create: `miniprogram/cloudfunctions/updateEvent/index.js`
- Create: `miniprogram/cloudfunctions/updateEvent/package.json`
- Create: `miniprogram/cloudfunctions/_shared/updateEvent.test.js`

`updateEvent` 的可改字段：`time`、`status`、`meet`、`starbucks`、`route`、`note`、`title`。禁止改 `id`、`date`。`meet`/`starbucks` 坐标逻辑与 `createEvent` 相同：有 lng/lat 就用，否则 geocode。

- [ ] **Step 1: 写失败测试（纯函数：剥出可改字段）**

把字段过滤做成 `updateEvent/index.js` 可导出的 `pickEventPatch`，测试先写在 `_shared` 以免云函数目录被测试依赖污染。

```js
// miniprogram/cloudfunctions/_shared/updateEvent.test.js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { pickEventPatch } = require('../updateEvent/index.js');

test('drops id and date', () => {
  const patch = pickEventPatch({
    id: 'hack',
    date: '2099-01-01',
    time: '08:00',
    title: '新标题',
    status: 'past',
    route: 'x',
    note: 'y',
  });
  assert.equal(patch.id, undefined);
  assert.equal(patch.date, undefined);
  assert.equal(patch.time, '08:00');
  assert.equal(patch.title, '新标题');
  assert.equal(patch.status, 'past');
});

test('rejects invalid status', () => {
  const patch = pickEventPatch({ status: 'nope' });
  assert.equal(patch.status, undefined);
});

test('passes meet and starbucks objects through', () => {
  const meet = { name: 'A', lng: 121.5, lat: 31.2 };
  const patch = pickEventPatch({ meet, starbucks: null });
  assert.deepEqual(patch.meet, meet);
  assert.equal(patch.starbucks, null);
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `node --test miniprogram/cloudfunctions/_shared/updateEvent.test.js`

Expected: 模块不存在或 `pickEventPatch` 未导出

- [ ] **Step 3: 实现 `updateEvent/index.js`**

`package.json`:

```json
{
  "name": "updateEvent",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~2.6.3",
    "@cloudbase/node-sdk": "~3.10.0"
  }
}
```

`index.js` 完整内容：

```js
const cloud = require('wx-server-sdk');
const https = require('https');
const { assertAdmin } = require('./assertAdmin');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

const AMAP_KEY = 'daa291695ce6f29af3b05436ab1122da';
const CITY = '上海';

function httpsGet(url) {
  return new Promise((resolve, reject) => {
    https.get(url, (res) => {
      let data = '';
      res.on('data', (chunk) => (data += chunk));
      res.on('end', () => {
        try {
          resolve(JSON.parse(data));
        } catch (e) {
          reject(new Error('JSON 解析失败: ' + e.message));
        }
      });
    }).on('error', reject);
  });
}

async function geocode(name) {
  if (!name) return null;
  try {
    const url = `https://restapi.amap.com/v3/geocode/geo?key=${AMAP_KEY}&address=${encodeURIComponent(name)}&city=${encodeURIComponent(CITY)}`;
    const res = await httpsGet(url);
    if (res.status === '1' && res.geocodes && res.geocodes.length > 0) {
      const g = res.geocodes[0];
      const [lng, lat] = g.location.split(',');
      return {
        name,
        address: g.formatted_address || name,
        lng: parseFloat(lng),
        lat: parseFloat(lat),
        poiid: null,
      };
    }
  } catch (e) {
    console.warn('geocode 失败', e);
  }
  return null;
}

async function resolvePlace(place) {
  if (place === null) return null;
  if (!place || !place.name) return undefined;
  if (place.lng && place.lat) {
    return {
      name: place.name,
      address: place.address || '',
      lng: place.lng,
      lat: place.lat,
      poiid: place.poiid || null,
    };
  }
  const geo = await geocode(place.name);
  return geo || {
    name: place.name,
    address: place.address || '',
    lng: null,
    lat: null,
    poiid: null,
  };
}

function pickEventPatch(input) {
  const patch = {};
  if (typeof input.time === 'string' && input.time) patch.time = input.time;
  if (input.status === 'upcoming' || input.status === 'past') patch.status = input.status;
  if (typeof input.title === 'string') patch.title = input.title;
  if (input.route === null || typeof input.route === 'string') patch.route = input.route || null;
  if (input.note === null || typeof input.note === 'string') patch.note = input.note || null;
  if (input.meet !== undefined) patch.meet = input.meet;
  if (input.starbucks !== undefined) patch.starbucks = input.starbucks;
  return patch;
}

exports.pickEventPatch = pickEventPatch;

exports.main = async (event) => {
  const { id } = event || {};
  if (!id) return { code: -1, msg: '缺少 id 参数' };

  const gate = await assertAdmin(cloud, db);
  if (!gate.ok) return { code: gate.code, msg: gate.msg };

  const existing = await db.collection('events').where({ id }).limit(1).get();
  if (existing.data.length === 0) return { code: -1, msg: '活动不存在' };

  const patch = pickEventPatch(event);
  if (patch.meet !== undefined) {
    const meet = await resolvePlace(patch.meet);
    if (meet === undefined) delete patch.meet;
    else patch.meet = meet;
  }
  if (patch.starbucks !== undefined) {
    const sbux = await resolvePlace(patch.starbucks);
    if (sbux === undefined) delete patch.starbucks;
    else patch.starbucks = sbux;
  }

  if (Object.keys(patch).length === 0) {
    return { code: -1, msg: '没有可更新的字段' };
  }

  await db.collection('events').where({ id }).update({ data: patch });
  const updated = await db.collection('events').where({ id }).limit(1).get();
  return { code: 0, msg: 'ok', data: { event: updated.data[0] } };
};
```

然后：`bash miniprogram/cloudfunctions/_shared/copy-assert-admin.sh`

- [ ] **Step 4: 跑测试**

Run: `node --test miniprogram/cloudfunctions/_shared/updateEvent.test.js`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add miniprogram/cloudfunctions/updateEvent miniprogram/cloudfunctions/_shared/updateEvent.test.js
git commit -m "feat(cloud): add updateEvent without changing id or date"
```

---

### Task 4: 云函数 `deletePhoto`

**Files:**
- Create: `miniprogram/cloudfunctions/deletePhoto/index.js`
- Create: `miniprogram/cloudfunctions/deletePhoto/package.json`
- Create: `miniprogram/cloudfunctions/_shared/deletePhoto.test.js`

- [ ] **Step 1: 写失败测试（过滤 photos 数组）**

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { removePhoto } = require('../deletePhoto/index.js');

test('removes matching fileID', () => {
  const photos = [
    { fileID: 'cloud://a.jpg' },
    { fileID: 'cloud://b.jpg' },
  ];
  const next = removePhoto(photos, 'cloud://a.jpg');
  assert.equal(next.length, 1);
  assert.equal(next[0].fileID, 'cloud://b.jpg');
});

test('returns null when fileID missing', () => {
  assert.equal(removePhoto([{ fileID: 'cloud://a.jpg' }], 'cloud://nope'), null);
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `node --test miniprogram/cloudfunctions/_shared/deletePhoto.test.js`

Expected: 模块不存在

- [ ] **Step 3: 实现**

`package.json` 与 `updateEvent` 相同，name 改为 `deletePhoto`。

```js
const cloud = require('wx-server-sdk');
const { assertAdmin } = require('./assertAdmin');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();
const _ = db.command;

function removePhoto(photos, fileID) {
  if (!Array.isArray(photos)) return null;
  const next = photos.filter((p) => p.fileID !== fileID);
  if (next.length === photos.length) return null;
  return next;
}

exports.removePhoto = removePhoto;

exports.main = async (event) => {
  const { eventId, fileID } = event || {};
  if (!eventId) return { code: -1, msg: '缺少 eventId' };
  if (!fileID || !String(fileID).startsWith('cloud://')) {
    return { code: -1, msg: 'fileID 不合法' };
  }

  const gate = await assertAdmin(cloud, db);
  if (!gate.ok) return { code: gate.code, msg: gate.msg };

  const res = await db.collection('events').where({ id: eventId }).limit(1).get();
  if (res.data.length === 0) return { code: -1, msg: '活动不存在' };

  const next = removePhoto(res.data[0].photos || [], fileID);
  if (!next) return { code: -1, msg: '照片不存在' };

  await db.collection('events').where({ id: eventId }).update({
    data: { photos: next },
  });

  try {
    await cloud.deleteFile({ fileList: [fileID] });
  } catch (err) {
    console.warn('云存储删除失败，数据库已更新', err);
  }

  return { code: 0, msg: 'ok', data: { photos: next } };
};
```

`copy-assert-admin.sh` 再跑一次。

- [ ] **Step 4: 跑测试**

Run: `node --test miniprogram/cloudfunctions/_shared/deletePhoto.test.js`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add miniprogram/cloudfunctions/deletePhoto miniprogram/cloudfunctions/_shared/deletePhoto.test.js
git commit -m "feat(cloud): add deletePhoto for admin photo removal"
```

---

### Task 5: `updateEventPhotos` 网站管理员跳过每人 9 张

**Files:**
- Modify: `miniprogram/cloudfunctions/updateEventPhotos/index.js`
- Create: `miniprogram/cloudfunctions/_shared/photoLimit.test.js`

- [ ] **Step 1: 写失败测试**

从 `updateEventPhotos/index.js` 导出 `shouldSkipPerUserLimit(kind)`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { shouldSkipPerUserLimit } = require('../updateEventPhotos/index.js');

test('web admin skips per-user 9 cap', () => {
  assert.equal(shouldSkipPerUserLimit('web'), true);
});

test('mini program runner does not skip', () => {
  assert.equal(shouldSkipPerUserLimit('mp'), false);
  assert.equal(shouldSkipPerUserLimit(null), false);
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `node --test miniprogram/cloudfunctions/_shared/photoLimit.test.js`

Expected: 未导出

- [ ] **Step 3: 改 `updateEventPhotos/index.js`**

顶部增加 `const { assertAdmin } = require('./assertAdmin');`

在限流「每人 9 张」之前调用 `assertAdmin`（该函数对跑友会返回 ok:false，不能因此拒绝上传）。正确写法：

```js
function shouldSkipPerUserLimit(kind) {
  return kind === 'web';
}

exports.shouldSkipPerUserLimit = shouldSkipPerUserLimit;
```

在 `myCount >= 9` 判断处：

```js
    const gate = await assertAdmin(cloud, db);
    const myCount = photos.filter((p) => p.uploader === openid).length;
    if (!shouldSkipPerUserLimit(gate.ok ? gate.kind : null) && myCount >= 9) {
      return { code: -1, msg: '你已经上传过 9 张了（每场活动最多 9 张）' };
    }
```

每场 100 张判断保持不变。`package.json` 加上 `@cloudbase/node-sdk`。跑 `copy-assert-admin.sh`。

注意：跑友上传时 `assertAdmin` 失败是正常的，不要 `return` 那个失败。

- [ ] **Step 4: 跑测试**

Run: `node --test miniprogram/cloudfunctions/_shared/photoLimit.test.js miniprogram/cloudfunctions/_shared/assertAdmin.test.js`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add miniprogram/cloudfunctions/updateEventPhotos miniprogram/cloudfunctions/_shared/photoLimit.test.js
git commit -m "feat(cloud): skip per-user photo cap for web admins only"
```

---

### Task 6: 脚手架 `admin/`（与 h5、miniprogram 同级）

**Files:**
- Create: `admin/package.json`
- Create: `admin/vite.config.ts`
- Create: `admin/tsconfig.json`
- Create: `admin/tsconfig.node.json`
- Create: `admin/index.html`
- Create: `admin/src/main.tsx`
- Create: `admin/src/App.tsx`
- Create: `admin/src/vite-env.d.ts`
- Create: `admin/src/styles.css`
- Create: `admin/.env.example`

- [ ] **Step 1: package.json**

```json
{
  "name": "run-spot-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudbase/js-sdk": "^2.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
```

`@cloudbase/js-sdk` 安装后核对大版本：必须用支持 `auth.signInWithPassword` 与 `auth.getSession` 的 v3 API。若 npm 上最新主版本是 3.x，改成 `"@cloudbase/js-sdk": "^3.0.0"`，不要用只有 `getLoginState` 的旧 API。

- [ ] **Step 2: Vite / TS / index.html**

`vite.config.ts`：

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: './',
  test: {
    environment: 'jsdom',
  },
});
```

`tsconfig.json`：`jsx: react-jsx`，`strict: true`，include `src`。  
`index.html` 挂 `#root`，script `/src/main.tsx`。  
`src/vite-env.d.ts`：

```ts
/// <reference types="vite/client" />
interface ImportMetaEnv {
  readonly VITE_CLOUDBASE_ENV_ID: string;
  readonly VITE_CLOUDBASE_REGION: string;
  readonly VITE_PUBLISHABLE_KEY: string;
}
```

`.env.example`：

```
VITE_CLOUDBASE_ENV_ID=run-spot-prod-d1gb2jd1j3ce2e7fb
VITE_CLOUDBASE_REGION=ap-shanghai
VITE_PUBLISHABLE_KEY=
```

`.env.local` 从 MCP `queryAppAuth(action="getPublishableKey")` 写入，不进 git。

- [ ] **Step 3: 最小 App 能编译**

`src/main.tsx` 渲染 `<HashRouter><App /></HashRouter>`。  
`src/App.tsx` 暂时返回 `<p>admin</p>`。  
`src/styles.css` 定义 `--color-brand: #C74634`、`--color-sbux: #006241`、`--bg: #FAFAFA`。

- [ ] **Step 4: 安装并构建**

Run:

```bash
cd admin && npm install && npx tsc --noEmit && npm run build
```

Expected: `tsc` 0 errors，`dist/` 生成

- [ ] **Step 5: Commit**

```bash
git add admin
git commit -m "chore(admin): scaffold Vite React TypeScript app"
```

---

### Task 7: SDK、session 守卫、api 封装

**Files:**
- Create: `admin/src/types.ts`
- Create: `admin/src/lib/cloudbase.ts`
- Create: `admin/src/lib/auth.ts`
- Create: `admin/src/lib/api.ts`
- Create: `admin/src/lib/auth.test.ts`
- Create: `admin/src/lib/api.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// admin/src/lib/auth.test.ts
import { describe, expect, it } from 'vitest';
import { sessionFromGetSession } from './auth';

describe('sessionFromGetSession', () => {
  it('treats missing session as logged out', () => {
    expect(sessionFromGetSession({ data: { session: undefined }, error: null }).loggedIn).toBe(false);
  });
  it('treats present session as logged in', () => {
    expect(sessionFromGetSession({ data: { session: { access_token: 'x' } }, error: null }).loggedIn).toBe(true);
  });
});
```

```ts
// admin/src/lib/api.test.ts
import { describe, expect, it } from 'vitest';
import { unwrapFunctionResult } from './api';

describe('unwrapFunctionResult', () => {
  it('returns msg from cloud function body', () => {
    const r = unwrapFunctionResult({ result: { code: -1, msg: '活动不存在' } });
    expect(r.ok).toBe(false);
    expect(r.msg).toBe('活动不存在');
  });
  it('ok when code is 0', () => {
    const r = unwrapFunctionResult({ result: { code: 0, msg: 'ok', data: { a: 1 } } });
    expect(r.ok).toBe(true);
    expect(r.data).toEqual({ a: 1 });
  });
});
```

- [ ] **Step 2: `cd admin && npm test`**

Expected: 模块找不到

- [ ] **Step 3: 实现**

`types.ts`：`Meet`、`Photo`、`EventItem`、`Team` 字段与云库一致（`meet.name/address/lng/lat/poiid`，`photos[].fileID` 等）。

`cloudbase.ts`：

```ts
import cloudbase from '@cloudbase/js-sdk';

const env = import.meta.env.VITE_CLOUDBASE_ENV_ID;
const region = import.meta.env.VITE_CLOUDBASE_REGION || 'ap-shanghai';
const accessKey = import.meta.env.VITE_PUBLISHABLE_KEY;

export const app = cloudbase.init({
  env,
  region,
  accessKey,
  auth: { detectSessionInUrl: true },
});

export const auth = app.auth;
```

禁止 `signInAnonymously`。禁止 `getLoginState`。

`auth.ts`：

```ts
export function sessionFromGetSession(res: { data?: { session?: unknown }; error?: unknown }) {
  return { loggedIn: Boolean(res.data?.session), error: res.error ?? null };
}
```

再导出 `async function readSession()` 内部只调用 `auth.getSession()` 后走 `sessionFromGetSession`。

`api.ts`：

```ts
export type FnResult<T> = { ok: true; data: T; msg: string } | { ok: false; data: null; msg: string };

export function unwrapFunctionResult<T>(res: { result?: { code?: number; msg?: string; data?: T } }): FnResult<T> {
  const body = res.result;
  if (!body) return { ok: false, data: null, msg: '云函数无返回' };
  if (body.code === 0) return { ok: true, data: body.data as T, msg: body.msg || 'ok' };
  return { ok: false, data: null, msg: body.msg || '请求失败' };
}

export async function callFn<T>(name: string, data?: Record<string, unknown>): Promise<FnResult<T>> {
  const res = await app.callFunction({ name, data: data || {} });
  return unwrapFunctionResult<T>(res);
}
```

`callFn` 需要 import `app` from `./cloudbase`。

- [ ] **Step 4: `cd admin && npm test`**

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add admin/src
git commit -m "feat(admin): CloudBase session guard and function wrapper"
```

---

### Task 8: 登录页 + 非管理员 uid 页 + 路由

**Files:**
- Create: `admin/src/pages/LoginPage.tsx`
- Create: `admin/src/pages/UnauthorizedPage.tsx`
- Modify: `admin/src/App.tsx`
- Create: `admin/src/pages/LoginPage.test.tsx`

- [ ] **Step 1: 登录页测试（无注册按钮）**

```tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { describe, expect, it } from 'vitest';
import { LoginPage } from './LoginPage';

describe('LoginPage', () => {
  it('has username, password, submit, and no register', () => {
    render(
      <MemoryRouter>
        <LoginPage />
      </MemoryRouter>,
    );
    expect(screen.getByLabelText('用户名')).toBeTruthy();
    expect(screen.getByLabelText('密码')).toBeTruthy();
    expect(screen.getByRole('button', { name: '登录' })).toBeTruthy();
    expect(screen.queryByText('注册')).toBeNull();
  });
});
```

- [ ] **Step 2: `cd admin && npm test`**

Expected: LoginPage 未定义

- [ ] **Step 3: 实现页面与路由**

`LoginPage`：`auth.signInWithPassword({ username, password })`，失败展示 `error.message`。成功后 `navigate('/')`。

`UnauthorizedPage`：props `{ uid: string }`，点击复制 `navigator.clipboard.writeText(uid)`。文案：把 uid 发给群主，加到 `team.webAdmins`。

`App.tsx` 用 `HashRouter` 已在 main。路由：

- `#/login` → LoginPage  
- `#/` 等受保护路由：先 `readSession()`，未登录 `<Navigate to="/login" />`  
- 已登录则调 `getEvents`，看返回的 `team.webAdmins` 是否包含当前 uid（uid 来自 `auth.getUser()` 或 session user；以 SDK 实际字段为准，通常 `data.user.uid`）。不包含 → UnauthorizedPage。包含 → Outlet。

获取 uid：登录后 `const { data } = await auth.getUser()`（若 SDK 无 getUser，用 `getSession()` 里 user.uid）。**不要**用 `getLoginState()`。

`checkWebAdmin(team, uid)` 抽到 `src/lib/events.ts` 便于测：`Array.isArray(team.webAdmins) && team.webAdmins.includes(uid)`。

- [ ] **Step 4: `cd admin && npm test && npx tsc --noEmit`**

Expected: PASS，0 errors

- [ ] **Step 5: Commit**

```bash
git add admin/src
git commit -m "feat(admin): login and unauthorized uid gate"
```

---

### Task 9: 活动列表

**Files:**
- Create: `admin/src/lib/events.ts`
- Create: `admin/src/lib/events.test.ts`
- Create: `admin/src/pages/EventListPage.tsx`
- Modify: `admin/src/App.tsx`（挂 `#/`）

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it } from 'vitest';
import { splitEvents, checkWebAdmin } from './events';

const events = [
  { id: '2026-09-26', date: '2026-09-26', _status: 'upcoming', title: 'A' },
  { id: '2026-09-12', date: '2026-09-12', _status: 'past', title: 'B' },
];

describe('splitEvents', () => {
  it('sorts upcoming asc and past desc', () => {
    const { upcoming, past } = splitEvents(events as never);
    expect(upcoming.map((e) => e.id)).toEqual(['2026-09-26']);
    expect(past.map((e) => e.id)).toEqual(['2026-09-12']);
  });
});

describe('checkWebAdmin', () => {
  it('matches uid', () => {
    expect(checkWebAdmin({ webAdmins: ['u1'] }, 'u1')).toBe(true);
    expect(checkWebAdmin({ webAdmins: ['u1'] }, 'u2')).toBe(false);
  });
});
```

- [ ] **Step 2: `cd admin && npm test`**

Expected: 失败

- [ ] **Step 3: 实现 `events.ts` 与列表页**

```ts
export function splitEvents(events: { id: string; date: string; _status: string }[]) {
  const upcoming = events
    .filter((e) => e._status === 'upcoming')
    .sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());
  const past = events
    .filter((e) => e._status === 'past')
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
  return { upcoming, past };
}

export function checkWebAdmin(team: { webAdmins?: string[] } | null, uid: string | null) {
  if (!team || !uid) return false;
  return Array.isArray(team.webAdmins) && team.webAdmins.includes(uid);
}
```

列表页：`callFn('getEvents')`，与小程序 `app.js` 一样给每条补 `_status`（手写 upcoming/past 优先，否则 `date >= today`）。筛选按钮：全部 / 即将 / 历史。卡片点进 `#/events/:id`。「+ 新活动」进 `#/events/new`。错误展示 `msg` 原文。

- [ ] **Step 4: `cd admin && npm test && npx tsc --noEmit`**

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add admin/src
git commit -m "feat(admin): event list with upcoming/past filters"
```

---

### Task 10: 新建 / 编辑表单 + 地址联想

**Files:**
- Create: `admin/src/components/AddressSuggest.tsx`
- Create: `admin/src/pages/EventFormPage.tsx`
- Create: `admin/src/lib/events.test.ts`（追加 date 不可改）

- [ ] **Step 1: 测试「编辑不能改日期」**

在 `events.ts` 增加：

```ts
export function isCreateRoute(pathname: string) {
  return pathname.endsWith('/events/new');
}
```

测试：`isCreateRoute('#/events/new')` 或传入 `location.hash` 规范化后的 path。编辑页日期字段 `readOnly`。

- [ ] **Step 2: 实现表单**

字段：日期、时间、状态 auto/upcoming/past、集合点名称+联想、集合点地址、星巴克名称+联想、路线、备注。

联想：输入防抖 300ms → `callFn('getAddressSuggestions', { keyword })` → 点击选项写入 name/address/lng/lat（location `"lng,lat"` 拆开）。

提交：

- new：`callFn('createEvent', { date, time, meet, starbucks, route, note, status })`，status 为 auto 时不传。成功跳转 `#/events/${id}`。  
- edit：`callFn('updateEvent', { id, time, meet, starbucks, route, note, title, status })`，**不传 date**。失败展示 `msg`（含「当天已有活动」「活动不存在」）。

删除活动：`confirm` 后 `callFn('deleteEvent', { id })`，成功回列表。

加载编辑：`callFn('getEvent', { id })` 或从 getEvents 找。

- [ ] **Step 3: `cd admin && npm test && npx tsc --noEmit`**

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add admin/src
git commit -m "feat(admin): create and edit events with address suggestions"
```

---

### Task 11: 照片墙

**Files:**
- Create: `admin/src/components/PhotoGrid.tsx`
- Create: `admin/src/lib/photos.ts`
- Create: `admin/src/lib/photos.test.ts`
- Modify: `admin/src/pages/EventFormPage.tsx`（仅编辑模式渲染 PhotoGrid）

- [ ] **Step 1: 测试部分失败合并**

```ts
import { describe, expect, it } from 'vitest';
import { mergeUploadResults } from './photos';

describe('mergeUploadResults', () => {
  it('keeps successes and lists failed indexes', () => {
    const r = mergeUploadResults(
      [{ fileID: 'cloud://a' }],
      [{ ok: true, photo: { fileID: 'cloud://b' } }, { ok: false, index: 1, msg: '太大' }],
    );
    expect(r.photos.map((p) => p.fileID)).toEqual(['cloud://a', 'cloud://b']);
    expect(r.failed).toEqual([{ index: 1, msg: '太大' }]);
  });
});
```

- [ ] **Step 2: 实现上传/删除**

`photos.ts`：`mergeUploadResults` 如上。

`PhotoGrid`：

- 用 `app.getTempFileURL({ fileList })`（SDK 方法名以已安装 js-sdk 为准，通常 `app.getTempFileURL` 或 `app.getTempFileURL({ fileList: ids })`）把 `fileID` 转 https。  
- 上传：`<input type="file" multiple accept="image/*">` → 每张 `app.uploadFile({ cloudPath: \`events/${eventId}/${date}_${rand}_${i}.jpg\`, filePath: file })` → `callFn('updateEventPhotos', { eventId, fileID, uploaderName: '后台管理员' })`。单张失败记入 `failed`，成功 merge。  
- 删除：`confirm` 后 `callFn('deletePhoto', { eventId, fileID })`，用返回的 photos 替换。

新建页不显示照片墙（还没有 id 文件目录也可，但 spec 写在编辑页）。

- [ ] **Step 3: `cd admin && npm test && npx tsc --noEmit && npm run build`**

Expected: 测试 PASS，build 成功

- [ ] **Step 4: Commit**

```bash
git add admin/src
git commit -m "feat(admin): upload and delete event photos"
```

---

### Task 12: 文档、第一个管理员、部署（不覆盖 H5）

**Files:**
- Create: `admin/README.md`
- Modify: `README.md`（项目结构加上 `admin/`，v5 改为进行中）
- Modify: `docs/superpowers/specs/2026-09-16-admin-web-design.md` 状态改为「实现中」仅当开始部署时；本任务写操作说明即可

- [ ] **Step 1: `admin/README.md`**

写清：`npm install && npm run dev`；`.env.local` 三个变量；登录无注册；如何把 uid 写入 `team.webAdmins`；部署必须用应用托管 `serviceName=admin`，**禁止** `tcb hosting:deploy` 到现有 H5 桶。

- [ ] **Step 2: 云函数部署**

用 MCP `manageFunctions`：

- `updateFunctionCode`：`createEvent`、`deleteEvent`、`updateEventPhotos`（`functionRootPath` 为 `miniprogram/cloudfunctions` 的绝对路径）
- `createFunction`：`updateEvent`、`deletePhoto`，`type: Event`，`runtime: Nodejs18.15`（若环境对已有函数锁在 16，新函数也用 Nodejs16.13 以减少差异）

删除线上残留 `adminLogin`：`manageFunctions(action="deleteFunction", functionName="adminLogin", confirm=true)`。

- [ ] **Step 3: 第一个 webAdmin**

1. `manageAppAuth(action="createUser")` 若 schema 不支持用户名密码创建，则在控制台「用户管理」手工建用户，密码交给群主。  
2. 用该账号在本地登录一次，Unauthorized 页复制 uid。  
3. MCP `writeNoSqlDatabaseContent`：`update` `team` `query: { id: "meta" }`，`$set: { webAdmins: ["<uid>"] }`（保留现有 `admins` 数组，用点号路径只设 `webAdmins`）。

- [ ] **Step 4: 托管 admin，不碰 H5**

`manageApps(action="deployApp", serviceName="admin", filePath="<repo>/admin", framework="static", installCmd="npm install", buildCmd="npm run build", buildPath="dist")`

部署后用 `queryHosting(action="websiteConfig")` 确认 H5 的 `*.tcloudbaseapp.com` 仍在。admin 应是 `admin-run-spot-prod-d1gb2jd1j3ce2e7fb.webapps.tcloudbase.com`。

CORS：`manageEnv(action="addSecurityDomain", domains=["admin-run-spot-prod-d1gb2jd1j3ce2e7fb.webapps.tcloudbase.com:443"])`。本地开发再加 `localhost:5173`。

- [ ] **Step 5: 浏览器验收清单（部署后执行，未完成不得声称上线）**

- 错误密码 → 展示 SDK 错误原文  
- 正确密码但 uid 不在 webAdmins → 只显示 uid  
- 写入 webAdmins 后 → 看到活动列表  
- 新建、编辑（日期只读）、删除活动（确认框）  
- 上传两张照片，删一张  
- 小程序 ⚙️ 原管理员仍能添加活动  
- H5 原域名仍打开群通知页  

- [ ] **Step 6: Commit 文档**

```bash
git add admin/README.md README.md
git commit -m "docs: add admin app usage and deploy notes"
```

ICP 未完成：**不要**在本任务绑定 `oracle.run.caolingxin.cn`。备案通过后另开任务：证书 + `manageGateway bindCustomDomain` + DNS CNAME。

---

## Spec coverage

| Spec 条目 | Task |
|---|---|
| `admin/` 与 h5、miniprogram 同级 | 6 |
| 用户名密码、无注册无匿名、getSession | 7, 8 |
| `team.webAdmins` + assertAdmin 双身份 | 1, 2, 12 |
| deleteEvent catch 放行修复 | 2 |
| updateEvent / deletePhoto | 3, 4 |
| 网站管理员跳过每人 9 张，100 张仍在 | 5 |
| 列表 / 新建 / 编辑 / 照片 | 9, 10, 11 |
| 错误展示 msg 原文、删除确认、部分上传失败 | 8, 10, 11 |
| 独立应用域名、不覆盖 H5、CORS | 12 |
| 删除 adminLogin | 12 |
| 自定义域名 | 12 明确推迟到备案后 |
| 不做成员/报名/H5 同步/改小程序配置页 | 全计划未包含 |

## 类型一致性

- `decideAdmin` / `assertAdmin` / `gate.ok` / `gate.kind` 为 `'mp' | 'web'`  
- 云函数统一 `{ code, msg, data }`，前端 `unwrapFunctionResult`  
- 活动 `id` 即日期；`pickEventPatch` 丢弃 `id`/`date`  
- 照片 `fileID` 必须以 `cloud://` 开头
